<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Edit Book</title>
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
  <h1>✏️ Edit <%= book.title %></h1>
  <form action="/edit/<%= book._id %>" method="POST">
    <label>Title:</label>
    <input type="text" name="title" value="<%= book.title %>" required>

    <label>Author:</label>
    <input type="text" name="author" value="<%= book.author %>" required>

    <label>Rating (1–10):</label>
    <input type="number" name="rating" min="1" max="10" value="<%= book.rating %>" required>

    <label>Notes:</label>
    <textarea name="notes"><%= book.notes %></textarea>

    <label>Date Read:</label>
    <input type="date" name="date_read" value="<%= book.date_read.toISOString().split('T')[0] %>" required>

    <button type="submit">Update Book</button>
  </form>
  <a href="/">← Back to Book List</a>
</body>
</html>

